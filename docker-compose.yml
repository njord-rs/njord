services:
  mysql:
    image: mysql:8.0
    container_name: njord_mysql
    environment:
      MYSQL_ROOT_PASSWORD: njord_rootpwd
      MYSQL_DATABASE: njord_db
      MYSQL_USER: njord_user
      MYSQL_PASSWORD: njord_password
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./njord_examples/mysql/init.sql:/docker-entrypoint-initdb.d/init.sql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
      - ./njord/db/test/mysql.sql:/docker-entrypoint-initdb.d/tests.sql

  oracle:
    image: gvenzl/oracle-free:latest
    container_name: njord_oracle
    environment:
      ORACLE_PASSWORD: njord_password
      APP_USER: test
      APP_USER_PASSWORD: test
    ports:
      - "1521:1521"
      - "5500:5500"
    volumes:
      - ./njord_examples/oracle/init_scripts:/container-entrypoint-initdb.d
      - ./njord/db/test/oracle:/container-entrypoint-initdb.d
    healthcheck:
      test: ["CMD", "healthcheck.sh"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 5s
      start_interval: 5s
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2019-latest
    container_name: njord_mssql
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=${Sa_Password:-njord_password}
      - MSSQL_PID=Developer
    ports:
      - 1433:1433
    volumes:
      - mssql_data:/var/opt/mssql
      - ./njord_examples/mssql/init_scripts:/container-entrypoint-initdb.d
      - ./njord/db/test/mssql:/container-entrypoint-initdb.d
    restart: always
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "/opt/mssql-tools/bin/sqlcmd -S localhost -U sa -P ${Sa_Password:-njord_password} -Q 'SELECT 1' || exit 1",
        ]
      interval: 10s
      retries: 10
      start_period: 10s
      timeout: 3s

volumes:
  mysql_data:
  oracle_data:
  mssql_data:
